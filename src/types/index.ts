/**
 * Tipos da loja usados na UI (cards, listagens, detalhe).
 * Fonte única: store-mappers (StoreProduct, StoreCategory).
 */
export type { StoreProduct as Product, StoreCategory as Category } from "@/lib/store-mappers";
