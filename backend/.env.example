# Torcida Urbana - Backend
# Copie para .env e ajuste. Para desenvolvimento local com docker-compose:
# cp .env.example .env   (depois rode: docker compose up -d  e  npm run db:push)
NODE_ENV=development
PORT=3000
# Padrão do docker-compose.yml deste repo (torcida/arquibancada)
DATABASE_URL="postgresql://torcida:torcida@localhost:5432/arquibancada?schema=public"
FRONTEND_ORIGINS="http://localhost:8080,http://localhost:5173,http://127.0.0.1:8080"

# Nuvemshop (quando integrar)
# NUVEMSHOP_STORE_ID=
# NUVEMSHOP_ACCESS_TOKEN=

# Chave para API admin (header X-Admin-Key)
ADMIN_API_KEY=torcida-urbana-admin-secret

# Login do cliente (JWT) - use uma string aleatória longa em produção
JWT_SECRET=torcida-urbana-jwt-secret-mude-em-producao

# Mercado Pago (Checkout Transparente / API Pagamentos)
# No painel do MP: Sua integração > Credenciais > Access Token e Public Key
MERCADOPAGO_ACCESS_TOKEN=TEST-ou-APP_USR-seu-token
MERCADOPAGO_PUBLIC_KEY=TEST-ou-APP_USR-sua-chave-publica
